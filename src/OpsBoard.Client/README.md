# OpsBoard Client

React/Vite frontend for the OpsBoard backend demo.

## Run

```powershell
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies `/api` requests to `http://localhost:5292`.

## Checks

```powershell
npm run lint
npm run build
```
